# 需求变更实施方案 20260603

## 变更来源
`D:\Github\tinysf-cc\docs\需求变更-20260603\20260603需求修改和补充.md`

---

## 一、变更摘要

| 区域 | 变更内容 |
|------|---------|
| 资料阶段 | 原 4 标签 → 2 标签：**文件解析** + **内容解析** |
| 需求阶段 | 去掉 **ORD 机会** 标签，保留 CRD / PRD / RTM |
| CRD 标题栏 | 新增参考资料下拉（已定稿资料勾选）+ md 文件上传 |
| CRD 生成按钮 | 无版本 → "生成 CRD"；有版本 → "重新生成 CRD" |
| CRD/PRD 编辑 | 对话聊天只改本地草稿；点「版本提交」才落库 |

---

## 二、文件解析标签详细流程

```
上传文件（click/drag）
  → 进入「待解析列表」（可全选/多选）
  → 点「批量解析」→ 触发 parseMaterial（file_id 模式）
  → 解析初稿展示（右侧预览 + 对话修改入口）
  → 每个文件可单独「定稿」（status=finalized）
  → 文件定稿后，内容解析标签中才可见该文件
```

## 三、内容解析标签详细流程

```
左列：已定稿文件列表（只读）
右列：
  [翻译] 下拉：英→中 / 法→中 / 中→英
  [索引] 一键建立
  [脱敏] 一键脱敏
  [知识库增强] 查知识库 / 联网查询 → 生成附件
  [对话修改] 任意修改当前文件内容
  [确认] 完成本文件内容解析
```
> 以上操作均非强制流程，按需点击；最后「确认」即可。

---

## 四、后端改动清单

### T-BE-01  文件上传临时存储
**新文件**：`backend/app/api/v1/files.py`

```
POST   /projects/{pid}/files/upload     multipart，存 BLOB_DIR/tmp/{pid}/
GET    /projects/{pid}/files            列出待解析文件（含已解析、已定稿）
DELETE /projects/{pid}/files/{fid}      删除
```
- 存储：`{BLOB_DIR}/tmp/{pid}/{uuid}_{原始文件名}`
- 元数据存 SQLite（新表 `uploaded_file`，字段：id/project_id/name/path/size/status/parsed_artifact_id/created_at）
- 启动时注册 `asyncio` 后台任务，每小时扫描清理 7 天前文件

### T-BE-02  资料定稿 + 内容解析扩展
**修改**：`backend/app/api/v1/materials.py`

```
POST /projects/{pid}/materials/{mid}/finalize
     → artifact.status = "finalized"

POST /projects/{pid}/materials/{mid}/transform
     body: { op: "translate"|"index"|"desensitize"|"enrich", params: {} }
     → 生成新 ArtifactVersion（stub 实现返回确定性结果）
     → 返回新版本 content
```
- `GET /projects/{pid}/materials` 返回增加 `status` 字段

### T-BE-03  工件参考资料关联 + 版本提交
**修改**：`backend/app/models/entities.py`  
- `Artifact` 新增 `extra: JSON` 字段（存 references 列表）

**修改**：`backend/app/api/v1/artifacts.py`
```
POST /projects/{pid}/artifacts/{aid}/references
     body: { materialIds: string[] }
     → 写入 artifact.extra.references

POST /projects/{pid}/artifacts/{aid}/submit-version
     body: { content: string, note?: string }
     → 新建 ArtifactVersion，current_version +1
     → 返回新版本号
```

### T-BE-04  初始化新表
**修改**：`backend/app/db_init.py`  
新增 `uploaded_file` 表的 create_all 支持（SQLAlchemy declarative 自动建表）

---

## 五、前端改动清单

### T-FE-01  api.ts 新增方法

```ts
uploadFile(pid, file: File) → UploadedFileDto
listFiles(pid) → UploadedFileDto[]
deleteFile(pid, fid) → void
finalizeMaterial(pid, mid) → MaterialDto
transformMaterial(pid, mid, op, params) → TransformResultDto
setReferences(pid, aid, materialIds) → void
submitVersion(pid, aid, content, note?) → { version: number }
```

新增类型：
```ts
UploadedFileDto { id, name, size, status, parsedArtifactId? }
TransformResultDto { version: number, content: string }
```

### T-FE-02  MaterialView.tsx 重构

两个顶级标签：
- **文件解析**（默认打开）
  - 左：已上传文件列表 + 上传按钮
  - 右上：待解析列表（多选/全选）+ 「批量解析」
  - 右下：选中文件预览（渲染/源码 segmented）+ 对话修改按钮 + 「定稿」
- **内容解析**
  - 左：已定稿文件列表（只读）
  - 右：操作面板（翻译/索引/脱敏/知识库/对话/确认）

### T-FE-03  RequirementView.tsx 修改

1. `SECTIONS` 去掉 `ord`，默认 `crd`
2. CRD/PRD 标题栏下方新增**参考资料栏**：
   - 多选下拉（已定稿 materials）+ 已选标签（可移除）
   - 上传 `.md` 文件按钮（非 md 格式提示走资料阶段）
   - 状态：默认自动加载已定稿资料
3. 生成按钮文本：`artifact ? "重新生成 CRD" : "生成 CRD"`
4. 新增「版本提交」按钮（调 `submitVersion`，与定稿分离）
5. 对话框修改：只更新 localContent state，不直接落库

---

## 六、任务优先级与顺序

```
T-BE-01 文件上传 API          ← 最先，MaterialView 依赖
T-BE-02 资料定稿/内容解析     ← 与 T-BE-01 并行
T-BE-03 工件参考 + 版本提交   ← 与 T-BE-01 并行
T-FE-01 api.ts 扩展           ← 后端接口定好后
T-FE-02 MaterialView 重构     ← 依赖 T-FE-01
T-FE-03 RequirementView 修改  ← 依赖 T-FE-01
验证                           ← 最后
```

---

## 七、不改动的部分

- RTM 矩阵页、设置页、Task 列表页、审计页——不变
- 后端 ORD 相关接口保留（不删），仅前端隐藏 ORD 标签
- SSE 六事件、HITL 三档、自检/定稿门——不变
- 鉴权、加密——不变
